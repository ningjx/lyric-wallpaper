# 核心依赖（网易云数据源 + 异步服务）
aiohttp>=3.9,<4        # 异步 HTTP 服务与客户端
pymem>=1.9.0           # 进程内存读取（播放进度/时长）
pywin32>=306           # 窗口标题枚举 + exe 版本资源读取
psutil>=5.9            # 读取 exe 路径识别版本
numpy>=1.24            # 偏移探测向量化扫描